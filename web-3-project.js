const specialswiper = new Swiper('#special-box-swiper', {
    spaceBetween:10,
    slidesPerView: "auto", 
    freeMode: true,
  
  
    // Navigation arrows
    navigation: {
      nextEl: '#next-box-desktop',
      prevEl: '#prev-box-desktop',
    },
  });
  const swiper = new Swiper('.swiper', {
    spaceBetween:10,
    slidesPerView: "auto", 
    freeMode: true,
  });